syntax = "proto3";

package kv;

service KeyValueService {
  rpc Get(Key) returns (Value) {}
  rpc Put(KeyValue) returns (Empty) {}
}

message Key {
  string key = 1;
}

message Value {
  string value = 1;
}

message KeyValue {
  string key = 1;
  string value = 2;
}

message Empty {}
